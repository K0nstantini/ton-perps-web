import { Typography } from "@mui/material";

export const NotAvailable = () => {
	return (
		<div>
			<Typography color='gray' style={{ fontStyle: 'italic' }}>not available</Typography>
		</div>
	)
};
